# Itinerary — REQ-2.1–2.4

Owner: Sanjay · Branch: `feat/itinerary` · SRS 4.2

Create a trip, build a day-wise itinerary, reorder stops.

`maps` reads the `trips` and `itinerary_items` tables this feature owns, so
their coordinate columns can't change without breaking Maps.

Task brief: [`team/sanjay/README.md`](../../../team/sanjay/README.md)

Only Sanjay edits this folder.
