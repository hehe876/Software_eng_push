# Itinerary — REQ-2.1–2.4

Flutter feature folder. Owner: Sanjay · Branch: `flex/itinerary` · SRS 4.2

Create a trip, build a day-wise itinerary, reorder stops.

`maps` reads the `Trip` and `ItineraryItem` models this feature owns, so
their fields can't change without breaking Maps — see
`lib/models/CONTRACT.md`.

Task brief: [`team/sanjay/README.md`](../../../team/sanjay/README.md)

Only Sanjay edits this folder.
